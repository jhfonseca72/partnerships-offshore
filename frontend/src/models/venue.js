class Venue {
    constructor(id, name, city, state) {
        this.id = id || '';
        this.name = name || '';
        this.city = city || '';
        this.state = state || '';
    }
}

export default Venue;